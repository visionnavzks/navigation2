# MPC 代价函数整理（完全体版）

这份文档主体按 smoother 本身的约束与代价连续编号展开。

速度模式和速度约束因为属于单独的速度规划层，不再占用主文编号，而是放到单独一节说明。

每一项都分别说明两个 smoother：

- 几何版 `Smoother`
- 运动学版 `KinematicSmoother`

每个 smoother 下都统一回答五件事：

1. 是否实现
2. 当前怎么实现
3. 数学原理是什么
4. 将来应该怎么实现
5. 现在有什么问题或限制

## 统一背景

### 1. 这两个 smoother 当前都不是完整时域 MPC

它们本质上都是非线性最小二乘：

$$
\min J(z) = \sum_k \lVert r_k(z) \rVert^2
$$

其中：

- $z$ 是优化变量
- $r_k$ 是每一类代价或约束写成的残差
- Ceres 实际最小化的是所有残差平方和

这也是代码里大量参数命名为 `*_weight_sqrt` 的原因：

- 先把平方根权重乘到残差上
- 再由 Ceres 统一平方
- 所以如果残差写成 $r = w_s e$，真正进入目标函数的是 $w_s^2 e^2$

### 2. 两个 smoother 的状态变量不同

几何版 `Smoother`：

- 优化变量是二维点列
- 可记为 $p_i = (x_i, y_i)$
- `yaw` 不是优化变量，而是求解结束后根据切向方向重建

运动学版 `KinematicSmoother`：

- 优化变量是显式状态链
- 可记为 $s_i = (x_i, y_i, θ_i, κ_i, ds_i)$
- 其中 $θ_i$ 是朝向，$κ_i$ 是曲率，$ds_i$ 是空间步长
- 每一段还带有 gear 语义：前进 $g_i = 1$，倒车 $g_i = -1$，cusp 停驻段 $g_i = 0$

### 3. 一个最重要的总判断

- 几何版更像“空间几何平滑器”
- 运动学版更像“可继续长成 MPC 的骨架”

所以：

- 只涉及几何形状、净空、参考线吸引力的需求，两版都能做
- 涉及 $θ / κ / gear / v / a / jerk / friction\ circle$ 这类显式车辆状态的需求，应优先甚至只在运动学版做
- 两版都支持 cusp 路径，但支持方式不同：
  - 几何版把 cusp 当成方向符号翻转点
  - 运动学版把 cusp 当成显式插入的停驻换向段

## 1. 起点约束

需求：起点 `x, y, theta, kappa`

### 几何版 `Smoother`

- 是否实现：部分实现
- 当前怎么实现：
  - 起点位置固定
  - `keep_start_orientation = true` 时，起点朝向固定
  - 起点 `kappa` 没有单独实现，因为几何版没有显式曲率状态
- 数学原理：
  - 起点位置本质是硬锚定，不作为自由变量漂移
  - 起点朝向：`keep_start_orientation = true` 通过固定边界点切向方向锁定，自动满足，结果是准的
  - 从优化变量角度看，几何版只优化 $p_i = (x_i, y_i)$，不优化显式 $κ_i$
- 应该怎么实现：
  - 位置和朝向：直接使用现有硬约束即可
  - 如果需要“起点曲率接近某值”，可以额外增加起点附近三点曲率残差或起点离散曲率残差
  - 如果需要严格固定起点 `kappa`，更适合放到运动学版里实现
- 问题和限制：
  - 位置和朝向均可硬约束；起点曲率没有显式状态，只能做近似几何约束

### 运动学版 `KinematicSmoother`

- 是否实现：部分实现
- 当前怎么实现：
  - 起点 `x, y` 固定
  - `keep_start_orientation = true` 时，起点 `θ` 固定
  - 起点 `kappa` 还没有单独边界项，但当前状态里已经有曲率 `κ`
- 数学原理：
  - 当前边界残差是一个二次残差
  - 起点位置约束可以写成

$$
r_{start,pos} = w_f [x_0 - x_0^{ref}, y_0 - y_0^{ref}]
$$

  - 如果启用朝向约束，再加上

$$
r_{start,heading} = w_f \cdot \mathrm{wrap}(θ_0 - θ_0^{target})
$$

  - 如果要固定起点曲率，可以直接增加

$$
r_{start,\kappa} = w_{\kappa 0} (κ_0 - κ_0^{target})
$$

- 应该怎么实现：
  - 不需要再额外引入新的转角状态
  - 直接在运动学版现有状态上增加起点曲率边界残差或参数边界即可
  - 如果想更硬一些，可以直接固定 `κ_0`
- 问题和限制：
  - 仅用 `κ` 时，表达的是几何/运动学曲率，不是机械执行器角度
  - 但对当前 smoother 目标来说，这通常已经足够

## 2. 终点约束

需求：

- 终点可以松弛，有一个范围
- 支持精准停、非精准停、修航向模式
- 参考目标点太贴障碍物时，能自动偏移到更合理目标点

### 几何版 `Smoother`

- 是否实现：部分实现
- 当前怎么实现：
  - 终点位置固定
  - `keep_goal_orientation = true` 时，终点朝向固定
  - 没有终点松弛区
  - 没有 heading-only 模式
  - 没有自动目标偏移
- 数学原理：
  - 当前终点本质是硬锚点，而不是软目标区
  - 精准停相当于固定终点位置
  - 朝向约束相当于固定终点切向方向
  - 但没有“区域内任意一点都可以”的数学模型
- 应该怎么实现：
  - 统一用目标点坐标系下的三轴容差约束，无需区分模式
  - 设目标朝向为 $θ_g$，旋转矩阵 $R_g = \begin{bmatrix} \cos θ_g & \sin θ_g \\ -\sin θ_g & \cos θ_g \end{bmatrix}$
  - 把终点偏差投影到目标坐标系

$$
\begin{bmatrix} δ_{lon} \\ δ_{lat} \end{bmatrix} = R_g (p_N - p_g)
$$

  - 位置部分（lon/lat 两轴）仍然可以写，因为 $R_g$ 只依赖 $θ_g$（固定参数），与优化变量无关
  - 两轴残差为

$$
r_{goal,lon} = w_{lon} \cdot \max(0,\ |δ_{lon}| - b_{lon})
$$

$$
r_{goal,lat} = w_{lat} \cdot \max(0,\ |δ_{lat}| - b_{lat})
$$

  - 朝向：直接使用现有硬约束（`keep_goal_orientation = true` 锁定边界点切向），自动满足，无需额外残差
  - 三种模式通过容差参数区分（几何版无朝向容差，θ 由硬约束开关控制）：

| 模式 | $b_{lon}$ | $b_{lat}$ | 朝向 |
|------|-----------|-----------|------|
| 精准停 | 0 | 0 | `keep_goal_orientation` 按需 |
| 非精准停 | $> 0$（有限阈值） | $> 0$（有限阈值） | 按需 |
| 航向精准 | $> 0$（有限阈值） | $> 0$（有限阈值） | `keep_goal_orientation = true` |
  - 自动目标偏移：不要直接塞进 smoother 目标函数，应该在 smoother 之前增加目标候选搜索
- 问题和限制：
  - 位置可以做带容差的软约束；朝向只有开关（硬约束或不约束），没有容差
  - 如果目标点本身贴障碍物，smoother 只能在约束冲突中失败，不能自发挑一个更安全的可行目标

### 运动学版 `KinematicSmoother`

- 是否实现：部分实现
- 当前怎么实现：
  - 终点 `x, y` 固定
  - `keep_goal_orientation = true` 时，终点 `θ` 固定
  - 终点 `ds` 被压到 0，更接近停车结束
  - 仍然没有终点松弛区、heading-only、自动目标偏移
- 数学原理：
  - 当前边界项可以写成

$$
r_{goal} = [w_f(x_N - x_N^{ref}), w_f(y_N - y_N^{ref}), w_f \cdot \mathrm{wrap}(θ_N - θ_N^{target}), w_f ds_N]
$$

  - 其中第三项是可选的，第四项用于把最后一步长压向 0
  - 这更像“停在指定终点，并结束运动”，不是“停在允许区域内”
- 应该怎么实现：
  - 扩展现有 `BoundaryCostFunctor` 或拆出新的 `GoalConstraintFunctor`
  - 统一用目标点坐标系下的三轴容差约束，无需区分模式
  - 设目标朝向为 $θ_g$，旋转矩阵 $R_g = \begin{bmatrix} \cos θ_g & \sin θ_g \\ -\sin θ_g & \cos θ_g \end{bmatrix}$
  - 把终点偏差投影到目标坐标系

$$
\begin{bmatrix} δ_{lon} \\ δ_{lat} \end{bmatrix} = R_g \begin{bmatrix} x_N - x_g \\ y_N - y_g \end{bmatrix}
$$

  - 三轴残差分别为

$$
r_{goal,lon} = w_{lon} \cdot \max(0,\ |δ_{lon}| - b_{lon})
$$

$$
r_{goal,lat} = w_{lat} \cdot \max(0,\ |δ_{lat}| - b_{lat})
$$

$$
r_{goal,θ} = w_θ \cdot \max(0,\ |\mathrm{wrap}(θ_N - θ_g)| - b_θ)
$$

  - 三种模式通过容差参数区分：

| 模式 | $b_{lon}$ | $b_{lat}$ | $b_θ$ |
|------|-----------|-----------|-------|
| 精准停 | 0 | 0 | 0 |
| 非精准停 | $> 0$（有限阈值） | $> 0$（有限阈值） | 按需 |
| 航向精准 | $> 0$（有限阈值） | $> 0$（有限阈值） | 0 |
  - 参数接口只需暴露 $(b_{lon}, b_{lat}, b_θ)$ 和对应权重，不需要 mode 枚举
  - 自动目标偏移建议放在 smoother 外层 orchestrator：
    - 候选点搜索
    - 评分函数综合 ESDF 净空、朝向误差、离原目标距离
- 问题和限制：
  - 当前“终点固定 + 障碍物净空”在某些场景会直接冲突，引入容差后可以缓解但不能完全消除
  - 容差参数和障碍物净空参数需要联合调，否则容差允许的落点可能仍然在障碍物内

## 3. 远离障碍物约束

需求：可选；有地图时启用，没有地图时可关闭

### 几何版 `Smoother`

- 是否实现：已实现
- 当前怎么实现：
  - 使用 ESDF 距离场
  - 支持单点检查、圆半径检查、局部 footprint checkpoints
  - cusp 邻域会提高障碍物权重
- 数学原理：
  - 先取世界点到最近障碍物的 signed distance，记为 $d$
  - 再减去足迹半径 $r_f$，得到表面净空

$$
d_{surf} = d - r_f
$$

  - 若要求安全净空 $d_{safe}$，当前惩罚是分段二次函数

$$
\phi_{obs}(d_{surf}) = 0, \text{if } d_{surf} \ge d_{safe}
$$

$$
\phi_{obs}(d_{surf}) = \left((d_{safe} - d_{surf}) / d_{safe}\right)^2, \text{if } d_{surf} < d_{safe}
$$

  - 对应残差可理解为

$$
r_{obs} = w_{obs} \cdot \phi_{obs}(d_{surf})
$$

- 应该怎么实现：
  - 这一项本身已经可用

  - 如果要支持“没有地图”，接口层应允许空 costmap 或显式关闭该项
- 问题和限制：
  - 当前更像净空惩罚，不是严格的碰撞硬约束
  - 权重太小会穿近障碍物，权重太大又容易导致优化发散或强烈保守

### 运动学版 `KinematicSmoother`

- 是否实现：已实现
- 当前怎么实现：
  - 对每个状态加障碍物残差
  - 使用状态里的 $θ_i$ 把局部 footprint checkpoints 旋转到世界系
  - cusp pose 使用更高障碍物权重
- 数学原理：
  - 与几何版同样是 ESDF 净空惩罚
  - 差别在于检查点从状态 $(x_i, y_i, θ_i)$ 直接变换出来，更接近真实 footprint
- 应该怎么实现：
  - 当前已经比较完整
  - 如果后续要叠加参考点带宽约束，建议把“障碍物净空”和“参考线项”拆成两个独立残差族
- 问题和限制：
  - 当前后验校验里若 `cost_check_radius <= 0`，逻辑会比优化阶段更宽松，工程上要注意一致性
  - 如果真的没有地图，现有 API 仍然要求有 costmap 对象

## 4. 参考线吸引力（含参考点带宽约束）

需求：

- 参考线可以是直线、圆弧
- 也可以是一组不规则点
- 也可以只要求离参考点不超过一定距离
- 可以有 cusp
- cusp 处允许原地打舵

### 几何版 `Smoother`

- 是否实现：部分实现
- 当前怎么实现：
  - 支持 cusp 路径，包括混合 A* 这类带换向点的参考路径
  - 已支持参考点列吸引
  - 已支持不规则点列
  - 已支持 cusp 作为方向切换点保留
  - 不支持直线/圆弧 primitive 参考
  - 当前不支持 cusp 原地打舵
- 数学原理：
  - 当前参考项是点对点吸引

$$
r_{ref,i} = w_r (p_i - p_i^{ref})
$$

  - 对应目标项是

$$
J_{ref} = \sum_i w_r^2 \lVert p_i - p_i^{ref} \rVert^2
$$

  - 如果不要求持续贴近，只要求“不要离参考点太远”，可以用同一锚点改写成带阈值的 hinge 版本

$$
r_{band,i} = w_b \cdot \max(0, \lVert p_i - p_i^{ref} \rVert - b_i)
$$

  - 因而“参考线吸引”和“参考点带宽约束”本质上是同一参考锚点上的两种 loss：一个是二次吸引，一个是超阈值才惩罚
  - 这不是点到参考曲线最短距离，而是点到参考采样点的二次偏差
  - cusp 在几何版里的语义只是“方向符号发生翻转”，不是“原地转向动作”
  - 因此几何版对 cusp path 的支持，本质上是“保留换向拓扑并在几何上平滑”，而不是“在 cusp 处优化一个显式停驻动作”
- 应该怎么实现：
  - 直线/圆弧 primitive 在输入时会先转成点列，所以当前点对点吸引框架可以直接复用，无需单独实现投影残差
  - 如果只想限制最大偏离距离，可以在现有参考点列上直接增加 $r_{band,i}$，不必再单开一节“道路边界约束”
  - 如果需求明确要求 cusp 处允许原地打舵，不建议在几何版做，因为它没有显式 `θ / κ` 状态
- 问题和限制：
  - 点对点吸引强依赖参考线采样密度
  - 参考点分布不均匀时，吸引力会不均匀
  - 带宽版本更像“参考线附近允许带”，不是严格的道路多边形边界
  - 没有显式 `kappa` 状态，就很难正确表达“cusp 停车后在原地重配曲率”

### 运动学版 `KinematicSmoother`

- 是否实现：部分实现
- 当前怎么实现：
  - 支持 cusp 路径，包括混合 A* 这类带前进/倒车切换的参考路径
  - 已支持参考点列吸引
  - 已支持不规则点列
  - 已支持 cusp；换向时插入显式停驻状态
  - 不支持直线/圆弧 primitive
  - 当前 cusp 还没有显式允许原地打舵
- 数学原理：
  - 当前参考项也是点对点吸引

$$
r_{ref,i} = w_r [x_i - x_i^{ref}, y_i - y_i^{ref}]
$$

  - 如果只要求状态中心不要偏离参考点太远，可改写成带宽版本

$$
r_{band,i} = w_b \cdot \max(0, \lVert c_i - p_i^{ref} \rVert - b_i)
$$

  - 其中 $c_i = (x_i, y_i)$ 是状态中心点
  - 这同样是“参考点锚定”的一类，只是 loss 从二次吸引改成了距离上界
  - 当前实现里，cusp 的数学含义还不是“原地打舵”，而是“位置不动、朝向不突变、步长接近 0”
  - 对 cusp 段，现有残差近似是

$$
r_{cusp} = [w_f(x_{i+1}-x_i), w_f(y_{i+1}-y_i), w_f \cdot \mathrm{wrap}(θ_{i+1}-θ_i), 0, 0, 10 w_{ds} ds_i]
$$

  - 因此运动学版对 cusp path 的支持，是把换向点扩成一个显式的“停驻换向过渡段”再做优化
  - 如果按需求允许 cusp 原地打舵，那么更合适的语义应是

$$
x_{i+1} = x_i, \quad y_{i+1} = y_i, \quad θ_{i+1} = θ_i, \quad ds_i \approx 0
$$

  - 同时允许 `κ` 在 cusp 两侧重新配置，而不强迫它连续

- 应该怎么实现：
  - 直线/圆弧 primitive 在输入时会先转成点列，当前点对点吸引框架可以直接复用
  - 如果只是需要“离参考点不超过一定距离”，直接在参考项里并入 $r_{band,i}$ 就够了
  - 由于需求已经明确允许 cusp 原地打舵，可以直接在 cusp 段附近放松 `κ` 连续性约束，或者专门增加 cusp 曲率过渡项
  - 这时仍然保持 $x, y, θ$ 不变，只允许 `κ` 在允许范围内重配
- 问题和限制：
  - 当前 cusp 只是停驻段，还不是“允许原地打舵”的完整语义
  - 仅用状态中心点做带宽约束时，仍然不是严格 footprint 级道路边界
  - 对 Ackermann 车来说，“原地转车身”本身也不合理，需要先定义车辆模型边界

## 5. 曲率能量

需求：不仅限制上界，还希望曲率整体更小、更平顺

### 几何版 `Smoother`

- 是否实现：近似实现
- 当前怎么实现：
  - 有一个三点二阶差分平滑项，间接压低局部弯折
  - 另有一个“超过最大曲率才惩罚”的上限项
  - 但没有显式写成 `κ` 的曲率能量项
- 数学原理：
  - 平滑项本质上是三点二阶差分

$$
r_{smooth,i} = w_s \cdot \left(\lambda (p_{i+1} - p_i) - (p_i - p_{i-1})\right)
$$

  - 等长采样时退化为

$$
r_{smooth,i} = w_s \cdot (p_{i+1} - 2p_i + p_{i-1})
$$

  - 它可以看成离散曲率的几何代理，因此会间接偏好更小的弯折
  - 真正的曲率上限项仍然是通过三点外接圆估计曲率

$$
κ_i ≈ 1 / R_i
$$

  - 再使用 hinge penalty

$$
r_{curv,limit} = w_c \cdot \max(0, κ_i - κ_{max})
$$

  - 所以当前实现是“二阶差分代理 regularization + 曲率上限 hinge penalty”
  - 它不是显式的 $κ$ 能量，但也不只是“超限才罚”
- 应该怎么实现：
  - 真正的曲率能量应直接惩罚曲率大小，例如

$$
r_{curv,energy} = w_k \cdot κ_i
$$

  - 或者对多个点的离散曲率平方求和
  - 最好和当前最大曲率上限同时保留：一个负责 regularization，一个负责 safety
- 问题和限制：
  - 当前对曲率的偏好来自几何代理项，不是显式 `κ` 状态
  - 这种写法数值上简单，但物理解释不如直接惩罚离散曲率清晰

### 运动学版 `KinematicSmoother`

- 是否实现：已实现
- 当前怎么实现：
  - 对相邻状态的平均曲率写一个残差
  - 再由 Ceres 自动平方后进入目标函数
  - 同时对状态变量 `κ` 直接施加上下界
- 数学原理：
  - 当前曲率能量项近似为

$$
r_{curv,i} = w_κ \cdot (κ_i + κ_{i+1}) / 2
$$

  - 由于 Ceres 会对残差平方求和，真正进入目标函数的是

$$
J_{curv,i} \propto w_κ^2 \cdot \left(\frac{κ_i + κ_{i+1}}{2}\right)^2
$$

  - 它可以看成对段中心曲率平方的离散近似
  - 但当前没有乘上 $ds_i$ 弧长权重，所以更准确地说是“等步长假设下的曲率正则”
  - `κ` 的上下界不是通过残差项实现，而是通过参数边界实现

$$
-κ_{max} \le κ_i \le κ_{max}
$$

- 应该怎么实现：
  - 当前工程上合理
  - 如果以后想更贴近 $\int κ^2 ds$，可把残差改成

$$
r_{curv,i} = w_κ \cdot \sqrt{ds_i} \cdot (κ_i + κ_{i+1}) / 2
$$

  - 如果以后需要更强的起终点曲率控制，可以把曲率正则和端点曲率边界一起考虑
- 问题和限制：
  - 当前正则的是相邻状态平均曲率，而不是单点曲率
  - 当前缺少显式 $ds_i$ 加权
  - 对当前 smoother 来说这是合理主变量，但如果未来要下钻到执行器层，会需要单独再做映射

## 6. 曲率变化率能量

需求：抑制曲率变化太快

### 几何版 `Smoother`

- 是否实现：未显式实现
- 当前怎么实现：
  - 没有独立的曲率变化率残差
  - 当前只有三点二阶差分平滑项，它主要抑制局部弯折
  - 该项不是严格的 $dκ/ds$
- 数学原理：
  - 当前起作用的仍然是二阶差分平滑项

$$
r_{smooth,i} = w_s \cdot \left(\lambda (p_{i+1} - p_i) - (p_i - p_{i-1})\right)
$$

  - 等长采样时就是

$$
r_{smooth,i} = w_s \cdot (p_{i+1} - 2p_i + p_{i-1})
$$

  - 它更像离散曲率或局部弯折的代理，而不是显式曲率导数
- 应该怎么实现：
  - 可以继续保留当前二阶差分项作为几何平滑正则
  - 如果想要真正的曲率变化率能量，应显式估计离散曲率，再对相邻曲率差分写残差
- 问题和限制：
  - 当前没有单独的 $dκ/ds$ 控制量
  - 调参时要把它理解为“几何平滑强度”，而不是严格的曲率变化率权重

### 运动学版 `KinematicSmoother`

- 是否实现：已实现
- 当前怎么实现：
  - 对相邻曲率差分做惩罚
  - 并用与 $ds_i$ 相关的尺度归一化
- 数学原理：
  - 当前残差近似是

$$
r_{dκ,i} = w_{dκ} \cdot (κ_{i+1} - κ_i) / \sqrt{\max(ds_i, ε)}
$$

  - 这个残差本身不是 $dκ/ds$，但经过 Ceres 平方后，目标函数对应

$$
J_{dκ,i} \propto w_{dκ}^2 \cdot \frac{(κ_{i+1} - κ_i)^2}{\max(ds_i, ε)}
$$

  - 这正对应离散化后的曲率变化率能量

$$
\int \left(\frac{dκ}{ds}\right)^2 ds
$$

  - 因而这里除以 $\sqrt{ds_i}$ 不是随意近似，而是为了让“平方后的代价”得到正确的弧长尺度
- 应该怎么实现：
  - 当前写法已经与离散曲率变化率能量一致
  - 真正可优化的方向主要是改进小步长处的数值保护和 $ε$ 选取
- 问题和限制：
  - 小步长段会天然受到更强惩罚，这通常符合预期，但会增强局部刚性
  - `ε` 的取值会影响极短段上的惩罚强度，需要结合采样间距调参

## 7. 最小转弯半径

需求：等价于最大曲率约束

### 几何版 `Smoother`

- 是否实现：已实现
- 当前怎么实现：
  - 优化阶段用超限惩罚
  - 求解后 validator 再做硬校验
- 数学原理：
  - 因为 $R_{min} = 1 / κ_{max}$
  - 所以限制最小转弯半径，等价于限制最大曲率
  - 当前优化阶段是软约束，后验是硬校验
- 应该怎么实现：
  - 当前已经够用
  - 如果一定要更硬，可以在某些变量上加几何参数化或投影，但实现复杂度很高，不建议先做
- 问题和限制：
  - 优化阶段不是硬边界，只是软惩罚
  - 仍然要依赖求解后校验兜底

### 运动学版 `KinematicSmoother`

- 是否实现：已实现
- 当前怎么实现：
  - 直接对 `κ_i` 施加参数边界
- 数学原理：

$$
-κ_{max} \le κ_i \le κ_{max}
$$

  - 这等价于

$$
R_i \ge 1 / κ_{max}
$$

  - 这是比几何版更硬的实现方式
- 应该怎么实现：
  - 当前实现已经是合理主线
  - 如果后续需要更严格的端点曲率约束，可以把端点 `κ` 也单独固定或限幅
- 问题和限制：
  - 当前边界针对的是 `κ`

## 8. 摩擦椭圆（摩擦圆为特例）

需求：

- 可能希望约束车身上某个偏置点，而不是重心点
- 与速度耦合

### 几何版 `Smoother`

- 是否实现：未实现
- 当前怎么实现：
  - 没有速度状态，因此实际上无从实现
- 数学原理：
  - 摩擦椭圆本质是横纵向动力学耦合，不是几何约束
  - 几何版没有 $v$，也没有 $a_{lon}$，自然无从表达
- 应该怎么实现：
  - 不建议在几何版实现
  - 应放到后续时域 MPC 或时间参数化层里
- 问题和限制：
  - 这项和几何版的建模层级不匹配

### 运动学版 `KinematicSmoother`

- 是否实现：未实现
- 当前怎么实现：
  - 当前没有速度状态，因此还没法写
- 数学原理：
  - 如果已经有速度与时间，并采用无侧偏的自行车模型近似，则重心处常见运动学量是

$$
ω = \dot ψ = v κ
$$

$$
α = \ddot ψ = \frac{d}{dt}(vκ) = \dot v \, κ + v \, \dot κ
$$

$$
a_{C} = \begin{bmatrix} a_{lon,C} \\ a_{lat,C} \end{bmatrix}
= \begin{bmatrix} \dot v \\ v^2 κ \end{bmatrix}
$$

  - 这里下标 $C$ 表示重心
  - 现在设要约束的不是重心，而是车身坐标系中一个固定偏置点 $P$

$$
\mathbf{r}_{CP} = \begin{bmatrix} x_p \\ y_p \end{bmatrix}
$$

  - 对平面刚体，点 $P$ 的加速度满足

$$
\mathbf{a}_{P} = \mathbf{a}_{C} + α \times \mathbf{r}_{CP} + ω \times (ω \times \mathbf{r}_{CP})
$$

  - 在车体系 $(x$ 前向，$y$ 左向$)$ 中展开后，有

$$
 a_{lon,P} = \dot v - α y_p - ω^2 x_p
$$

$$
 a_{lat,P} = v^2 κ + α x_p - ω^2 y_p
$$

  - 再代入 $ω = vκ$ 与 $α = \dot v \, κ + v \, \dot κ$，可写成

$$
 a_{lon,P} = \dot v - (\dot v \, κ + v \, \dot κ) y_p - (vκ)^2 x_p
$$

$$
 a_{lat,P} = v^2 κ + (\dot v \, κ + v \, \dot κ) x_p - (vκ)^2 y_p
$$

  - 如果更喜欢空间域写法，利用 $\dot κ = v \, dκ/ds$，也可写成

$$
 α = \dot v \, κ + v^2 \frac{dκ}{ds}
$$

  - 于是“偏置点摩擦椭圆”应直接对点 $P$ 的加速度施加二次约束

$$
\left(a_{lon,P} / a_{lon,max}\right)^2 + \left(a_{lat,P} / a_{lat,max}\right)^2 \le 1
$$

  - 当 $a_{lon,max} \ne a_{lat,max}$ 时，这是标准摩擦椭圆
  - 当 $a_{lon,max} = a_{lat,max} = a_{total}$ 时，它退化为摩擦圆

$$
a_{lon,P}^2 + a_{lat,P}^2 \le a_{total}^2
$$

  - 代入上面的展开式，可得

$$
\left(\frac{\dot v - α y_p - ω^2 x_p}{a_{lon,max}}\right)^2 +
\left(\frac{v^2 κ + α x_p - ω^2 y_p}{a_{lat,max}}\right)^2 \le 1
$$

  - 如果写成软惩罚，可写成

$$
r_{fric} = w_f \cdot \max\left(
0,
\left(\frac{\dot v - α y_p - ω^2 x_p}{a_{lon,max}}\right)^2 +
\left(\frac{v^2 κ + α x_p - ω^2 y_p}{a_{lat,max}}\right)^2 - 1
\right)
$$

  - 这里的“偏置”不是常量平移偏置
  - 它本质上是刚体上偏置点相对重心多出来的切向项 $α \times r$ 和向心项 $ω \times (ω \times r)$
  - 所以它是一个随 $v, \dot v, κ, \dot κ$ 变化的状态相关修正，而不是固定的 $(b_{lon}, b_{lat})$
- 应该怎么实现：
  - 先引入速度状态和时间维
  - 再明确摩擦约束到底施加在重心、后轴、前轴还是车身某个监测点
  - 若是偏置点，就按上式把该点加速度显式展开后再入约束
  - 如果未来再考虑载荷转移，可进一步把 $a_{lon,max}$、$a_{lat,max}$ 做成状态相关量
- 问题和限制：
  - 这已经明显超出当前“空间域运动学 smoother”的范围，属于真正 MPC 的领域
  - 仅靠空间域路径变量无法正确表达 $\dot v$ 和摩擦约束
  - 如果偏置点不在重心，约束里还会自然引入 $\dot κ$ 或 $dκ/ds$，比重心摩擦圆多一层动态耦合

## 最后总结

按你这份“完全体版”需求来判断：

- 几何版 `Smoother`
  - 明确支持 cusp 路径，但 cusp 的语义是“方向翻转点”
  - 已经能覆盖：起终点位置/朝向、参考路径吸引、障碍物净空、最大曲率、几何高阶平滑、cusp 方向切换
  - 适合做：空间几何优化
  - 不适合做：速度模式、速度约束、摩擦圆这类时域或动力学层约束

- 运动学版 `KinematicSmoother`
  - 明确支持 cusp 路径，但 cusp 的语义是“显式停驻换向段”
  - 已经能覆盖：起终点位置/朝向、离散运动学一致性、显式曲率与曲率变化率、最小转弯半径、gear 一致性、cusp 停驻、参考线吸引、障碍物净空
  - 适合做：继续往 MPC 扩展的主干
  - 当前缺口主要在：目标松弛区、参考线的带宽版本、primitive 参考线、cusp 原地打舵语义，以及与外部速度规划的接口边界

如果按实现优先级排，我建议顺序是：

1. 先做运动学版终点模式化约束：精准停 / 范围停 / heading-only
2. 再把参考点距离上界版本并入运动学版参考线项
3. 再做 primitive 参考线
4. 再补端点 `kappa` 约束和 cusp 原地打舵语义
5. 速度模式、速度约束、摩擦圆单独放到速度规划或时域 MPC 层处理

原因很简单：

- 前三项仍然主要是空间域问题，和当前架构连续
- 端点 `kappa` 和 cusp 原地打舵语义仍然可以在现有状态空间里解决
- 速度与摩擦圆会把当前运动学 smoother 推向真正的时域 MPC，复杂度会明显上一个台阶
- 如果当前项目阶段是“先把几何 smoother 做稳”，那 9 和 10 最好不要混进主优化问题里

## 速度规划相关

需要单独的速度规划器

### 速度模式

需求：高速、适中、精度三种风格

### 速度约束

需求：

- 最大速度、加速度、加加速度
- 支持倒车速度约束

### 几何版 `Smoother`

- 是否实现：基本未实现
- 当前怎么实现：
  - 只有方向语义：路径点第三分量可以表示前进或倒车
  - cusp 可表示换向
  - 但没有 $v / a / jerk$
- 数学原理：
  - 几何版没有时间变量，也没有速度变量
  - 因此无法写出

$$
v_{i+1} = v_i + a_i Δt_i
$$

  - 也无法写出 jerk 约束
- 应该怎么实现：
  - 不建议在几何版里做完整速度约束
  - 更好的分层是：
    - 先用几何版求空间路径
    - 再单独做速度规划或时间参数化
- 问题和限制：
  - 如果硬把速度问题塞进几何版，会把一个简单空间平滑器改成四不像

### 运动学版 `KinematicSmoother`

- 是否实现：部分实现
- 当前怎么实现：
  - 已有 gear 前进/倒车语义
  - 已有非 cusp 段“位姿变化方向必须和 gear 一致”的后验检查
  - 但仍没有 $v / a / jerk$
- 数学原理：
  - 当前只保证“往前段就沿 heading 前进，倒车段就沿 heading 反向移动”
  - 并没有真正的速度演化方程
- 应该怎么实现：
  - 如果要升级成接近 MPC 的形式，推荐引入

$$
s_i = (x_i, y_i, θ_i, κ_i, v_i, a_i, Δt_i)
$$

  - 再加

$$
v_{i+1} = v_i + a_i Δt_i
$$

$$
a_{i+1} = a_i + j_i Δt_i
$$

  - 约束项可以写成

$$
r_v = w_v \cdot \max(0, |v_i| - v_{max})
$$

$$
r_a = w_a \cdot \max(0, |a_i| - a_{max})
$$

$$
r_j = w_j · (a_{i+1} - a_i)
$$

  - 倒车速度也可单独设上下界
- 问题和限制：
  - 一次性把空间平滑、运动学约束、时间参数化全塞进一个求解器，复杂度会上升很快
  - 更务实的做法常常是“空间平滑 + 时间参数化”分两层做
